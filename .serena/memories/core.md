# Core

- Laravel app rooted at `D:\repogist\QuanLyNhaTro`.
- Main domain: room rental management (`Room`, `Resident`, `UtilityRecord`, `Bill`, `Contract`, `Equipment`, `Ticket`).
- Web UI routes live in `routes/web.php`; API/mobile-style routes live in `routes/api.php`; ad-hoc scheduled command definitions live in `routes/console.php`.
- Admin web screens are Blade files under `resources/views/admin/*`; admin dashboard is a large single Blade file with tab sections.
- Read stack/tooling details in `mem:tech_stack`; read commands in `mem:suggested_commands`; read local coding conventions in `mem:conventions`; read done checks in `mem:task_completion`.